#include "stdafx.h"
/*
#include "barRenderer.h"

#include "GLAbstractions/vao.h"
#include "GLAbstractions/vbo.h"
#include "GlAbstractions/vertexAttribute.h"
#include "Graphics/barBuffer.h"

BarRenderer::BarRenderer()
	: AbstractRenderer()
{
}

BarRenderer::~BarRenderer()
{
	// Nothing todo here
}

void BarRenderer::createGPUBuffers()
{
	// TODO : Plan is to do a set of buffers
	// for textured and non-textured data
	VAO* simpleVAO = new VAO("VAO for nontextured data.");
	VBO* vertexVBO = new VBO("VBO for nontextured vertices.");
	vbos_.push_back(vertexVBO);
	VBO* normalVBO = new VBO("VBO for nontextured normals");
	vbos_.push_back(normalVBO);
	VBO* colourVBO = new VBO("VBO for nontextured colours.");
	vbos_.push_back(colourVBO);

	// TODO : Make the stuff above be a map maybe,
	// or simply store them as instance variables instead
	// of vectors so its easier to reference them.

	// TODO: Make sure these are delete properly
	// VertexAttribute
}
*/
